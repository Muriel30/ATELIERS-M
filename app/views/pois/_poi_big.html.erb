<div class="card card-big" style="background-image: url(<%= cl_image_path poi.photo.path, width: 800, height: 600, crop: :fill %>)">

  <div class="card-cta">
    <%= link_to "Visiter <br><strong>#{poi.title}</strong>".html_safe, poi_path(poi), class: "btn btn-default btn-card-big" %>
  </div>

</div>
